# test-snapshot.py (host): nenhuma dependencia externa.
# Usa so a biblioteca padrao do Python (socket + http.client) para falar
# com o socket Unix da API do Firecracker. Nao precisa de pip install.
