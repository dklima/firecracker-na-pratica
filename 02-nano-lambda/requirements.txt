# Host (nano-lambda.py): nenhuma dependencia externa.
# O script usa so a biblioteca padrao do Python (socket, http.client, json).
#
# Dentro da VM (handler.py): qrcode e Pillow, instalados no rootfs
# pelo build-rootfs.sh via apk (py3-qrcode, py3-pillow). Nao via pip.
