# Host (nano-lambda-network.py): nenhuma dependencia externa.
# O script usa so a biblioteca padrao do Python (socket, http.client, json).
#
# Dentro da VM (handler.py): requests e ca-certificates, instalados no rootfs
# pelo build-rootfs-network.sh via apk (py3-requests, ca-certificates). Nao via pip.
